const pieceReducer = (state = initialState, action) => {
    switch (action.type){
        case 'LOADING_PIECES':

        case 'GET_PIECES':

        default:
            return state
    }
}

export default pieceReducer